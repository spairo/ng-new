sizes = 0 3 5 8 10 11 12 13 14 15 16 18 20 22 24 25 30 35 40 45 48s 50 55 60 70 80 100 120 150

for num in sizes
	.mb-{num}
		margin-bottom: unit(num, px)
	.mt-{num}
		margin-top: unit(num, px)
	.ml-{num}
		margin-left: unit(num, px)
	.mr-{num}
		margin-right: unit(num, px)

	.pb-{num}
		padding-bottom: unit(num, px)
	.pt-{num}
		padding-top: unit(num, px)
	.pl-{num}
		padding-left: unit(num, px)
	.pr-{num}
		padding-right: unit(num, px)

	// Negativos
	.m-b-{num}
		margin-bottom: -(unit(num, px))
	.m-t-{num}
		margin-top: -(unit(num, px))
	.m-l-{num}
		margin-left: -(unit(num, px))
	.m-r-{num}
		margin-right: -(unit(num, px))

	// Tamaño de texto
	.txt{num}
		font-size: unit(num, px)
